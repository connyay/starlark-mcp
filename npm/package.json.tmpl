{
  "name": "${node_pkg}",
  "version": "${node_version}",
  "description": "Platform-specific binary for starlark-mcp (${node_os}-${node_arch})",
  "repository": {
    "type": "git",
    "url": "https://github.com/anthropics/starlark-mcp.git"
  },
  "license": "MIT",
  "os": ["${node_os}"],
  "cpu": ["${node_arch}"]
}
